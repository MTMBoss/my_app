import 'package:flutter/material.dart';

class BigliettiPage extends StatelessWidget {
  const BigliettiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Biglietti', style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
